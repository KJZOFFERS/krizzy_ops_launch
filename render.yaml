services:
  - type: web
    name: krizzy-ops-web
    env: node
    plan: free
    buildCommand: "cd web && npm ci"
    startCommand: "cd web && node server.js"
    envVars:
      - key: PORT
        value: "10000"
      - key: DISCORD_WEBHOOK_OPS
        sync: false
