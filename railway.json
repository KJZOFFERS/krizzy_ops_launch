{
  "build": {
    "builder": "NIXPACKS"
  },
  "services": {
    "api": {
      "startCommand": "uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080}"
    },
    "worker": {
      "startCommand": "python worker.py"
    }
  }
}
